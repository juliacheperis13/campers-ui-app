// export const selectLocation = (state) => state.filters.allFilters.location;

// export const selectEquipmentList = (state) => state.filters.allFilters.equipmentList;

// export const selectTypeList = (state) => state.filters.allFilters.typeList;

// export const selectSelectedType = (state) =>
//   state.filters.allFilters.typeList.filter((item) => item.isSelected);

// export const selectSelectedEquipment = (state) =>
//   state.filters.allFilters.equipmentList.filter((item) => item.isSelected);

export const selectAppliedFilters = (state) => state.filters;